{# <form method="post" action="{{ path('app_web_album_delete', {'id': album.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ album.id) }}">
    <button type="submit" class="btn btn-danger">Supprimer</button>
</form> #}

<form method="post" action="{{ path('app_web_album_delete', {'id': album.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ album.id) }}">
    <button type="submit" class="btn btn-danger">Supprimer</button>
</form>