{% extends 'base.html.twig' %}

{% block title %}Artiste{% endblock %}

{% block body %}
    <h1>Artiste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ artist.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_web_artist_index') }}">Revenir à la liste</a>

    <a href="{{ path('app_web_artist_edit', {'id': artist.id}) }}">Modifier</a>

    {{ include('web/artist/_delete_form.html.twig') }}
{% endblock %}
